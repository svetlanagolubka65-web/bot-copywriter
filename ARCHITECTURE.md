# Архитектура: ИИ-помощник по контенту (bot.py)

## Стек

| Компонент | Технология |
|-----------|-----------|
| Язык | Python 3.10+ |
| Telegram | python-telegram-bot 21.5 (async) |
| ИИ-модель | Groq API → Llama 3.3 70B Versatile |
| Транскрипция голоса | Groq Whisper Large v3 |
| Хранилище пользователей | `users.json` (локальный файл) |
| Конфиг | `.env` через python-dotenv |

## Как работает

```
Пользователь (Telegram)
        │
        ▼
  handle_message()
  handle_voice()
  handle_callback()
        │
        ▼
  ask_next_question()  ←── CONTENT_TYPES[тип]["questions"]
        │
        ▼ (после сбора всех ответов)
  generate_from_answers()
        │
        ▼
  Groq API (Llama 3.3 70B)
        │
        ▼
  Сообщение пользователю + result_keyboard()
```

## Ключевые структуры

### CONTENT_TYPES
Словарь с 6 типами контента. Каждый тип содержит:
- `questions` — список уточняющих вопросов (текст + опционально кнопки)
- `prompt` — шаблон промпта с `{плейсхолдерами}`
- `label` — название для логов

### Поток сообщений

1. Пользователь нажимает кнопку меню → `content_type` сохраняется в `user_data`
2. Бот задаёт вопросы по очереди через `ask_next_question()`
3. Ответы сохраняются в `user_data["answers"]` по ключу вопроса
4. После последнего вопроса → `generate_from_answers()`
5. Промпт собирается через `prompt.format(**answers)`
6. Если пользователь загрузил стиль через `/settov` — он добавляется в системный промпт

### Callback-кнопки после генерации

| callback_data | Действие |
|--------------|----------|
| `regenerate` | Повторная генерация с теми же ответами |
| `refine` | Запрашивает правки, перегенерирует с учётом |
| `copy` | Отправляет чистый текст без кнопок |
| `main_menu` | Очищает `user_data`, возвращает в меню |
| `restart` | То же что main_menu |

### Голосовой ввод
1. `handle_voice()` скачивает аудио через Telegram API
2. Передаёт в Groq Whisper → получает текст
3. Сохраняет текст как `answers["topic"]`
4. Запускает поток вопросов для типа `🎙 Голосовой ввод` (только аудитория и тон)

### Стиль автора (ToV)
- Команда `/settov` → `user_data["waiting_tov"] = True`
- Следующий текст от пользователя сохраняется в `user_data["tov"]`
- При генерации `tov` добавляется в системный промпт: `"Стиль автора:\n{tov}"`

## Хранение данных

- **`users.json`** — список Telegram ID пользователей (для различения новых и вернувшихся)
- **`user_data`** — in-memory контекст разговора (сбрасывается при /start или restart)

## Ограничения

- `user_data` не персистентный — после перезапуска бота стиль (ToV) сбрасывается
- `users.json` хранится локально — при деплое нужно либо монтировать volume, либо перейти на БД
